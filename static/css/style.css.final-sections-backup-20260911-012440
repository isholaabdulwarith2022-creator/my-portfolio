* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    background: radial-gradient(circle at 10% 10%, rgba(124, 92, 255, 0.10), transparent 28%), radial-gradient(circle at 90% 20%, rgba(0, 210, 255, 0.08), transparent 25%), radial-gradient(circle at 70% 85%, rgba(255, 60, 190, 0.07), transparent 28%), #050608;
    color: #f7f8fc;
    line-height: 1.6;
}

body.modal-open {
    overflow: hidden;
}

a {
    color: inherit;
    text-decoration: none;
}

button {
    font: inherit;
}

.container {
    width: min(1120px, 92%);
    margin: 0 auto;
}


/* ==================== NAVIGATION ==================== */

.navbar {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(5, 6, 8, 0.88);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(18px);
}

.nav-container {
    min-height: 76px;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.logo {
    font-size: 1.2rem;
    font-weight: 800;
    letter-spacing: 1px;
}

.logo span,
footer strong span {
    color: #aeb5c3;
}

nav {
    display: flex;
    align-items: center;
    gap: 28px;
}

nav a {
    color: #aeb5c3;
    font-size: 0.9rem;
    transition: 0.3s ease;
}

nav a:hover {
    color: #ffffff;
}

.nav-contact {
    padding: 10px 18px;
    border: 1px solid rgba(124, 92, 255, 0.65);
    border-radius: 10px;
    color: #ffffff;
}

.menu-toggle {
    display: none;
    border: none;
    background: transparent;
    color: white;
    font-size: 1.7rem;
    cursor: pointer;
}


/* ==================== HERO ==================== */

.hero {
    min-height: 88vh;
    display: flex;
    align-items: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.hero-content {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 80px;
    align-items: center;
    padding: 100px 0;
}

.hero-text {
    min-width: 0;
}

.eyebrow,
.section-label {
    color: #00d9ff;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 3px;
    margin-bottom: 20px;
}

.hero h1 {
    max-width: 750px;
    font-size: clamp(3rem, 6vw, 5.6rem);
    line-height: 1.02;
    letter-spacing: -4px;
}

.hero h1 span {
    display: block;
    color: #929aaa;
}

.hero-description {
    max-width: 620px;
    color: #aeb5c3;
    font-size: 1.1rem;
    margin: 30px 0;
}

.hero-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
}

.btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 24px;
    border-radius: 10px;
    font-weight: 700;
    font-size: 0.9rem;
    transition: 0.3s ease;
}

.btn-primary {
    background: linear-gradient(135deg, #7c5cff, #b44dff 52%, #ff4ecd);
    color: #ffffff;
}

.btn-primary:hover {
    transform: translateY(-3px);
}

.btn-secondary {
    border: 1px solid rgba(255, 255, 255, 0.14);
    color: #ffffff;
}

.btn-secondary:hover {
    border-color: #00d9ff;
}

.hero-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 38px;
}

.hero-tech span,
.project-tech span,
.skills-list span {
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 5px;
    padding: 7px 11px;
    color: #a9a9a9;
    font-size: 0.78rem;
}


/* ==================== PROFILE ==================== */

.hero-visual {
    display: flex;
    justify-content: center;
}

.profile-card {
    width: 330px;
    padding: 20px;
    border: 1px solid #2c2c2c;
    background: linear-gradient(145deg, rgba(20, 23, 31, 0.92), rgba(10, 12, 17, 0.92));
    border-radius: 14px;
}

.profile-image {
    width: 100%;
    height: 360px;
    overflow: hidden;
    background: linear-gradient(135deg, rgba(124,92,255,0.14), rgba(0,217,255,0.08), rgba(255,78,205,0.10));
    border-radius: 10px;
    margin-bottom: 22px;
}

.profile-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.profile-info h3 {
    font-size: 1.25rem;
}

.profile-info p {
    color: #929aaa;
}

.status {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 18px;
    color: #8e8e8e;
    font-size: 0.8rem;
}

.status span {
    width: 8px;
    height: 8px;
    background: linear-gradient(135deg, #7c5cff, #b44dff 52%, #ff4ecd);
    border-radius: 50%;
}


/* ==================== SECTIONS ==================== */

.section {
    padding: 110px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.section-dark {
    background: rgba(13, 16, 22, 0.82);
}

.section-heading {
    max-width: 760px;
    margin-bottom: 55px;
}

.section-heading h2 {
    font-size: clamp(2rem, 4vw, 3.6rem);
    line-height: 1.1;
    letter-spacing: -2px;
}

.section-heading > p:last-child {
    color: #a6adba;
    margin-top: 18px;
}


/* ==================== ABOUT ==================== */

.about-grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 70px;
}

.about-text p {
    color: #aeb5c3;
    font-size: 1.05rem;
    margin-bottom: 22px;
}

.about-highlight {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1px;
    background: #333333;
    border: 1px solid rgba(255, 255, 255, 0.09);
}

.highlight {
    background: linear-gradient(145deg, rgba(20, 23, 31, 0.92), rgba(10, 12, 17, 0.92));
    min-height: 150px;
    padding: 25px;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.highlight strong {
    font-size: 1.4rem;
}

.highlight span {
    color: #929aaa;
}


/* ==================== SERVICES ==================== */

.services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
}

.service-card {
    border: 1px solid #2c2c2c;
    padding: 35px;
    transition: 0.3s ease;
}

.service-card:hover {
    transform: translateY(-5px);
    border-color: rgba(124, 92, 255, 0.48);
}

.service-number {
    color: #7c5cff;
    font-size: 0.8rem;
    margin-bottom: 30px;
}

.service-card h3 {
    font-size: 1.4rem;
    margin-bottom: 15px;
}

.service-card p {
    color: #a3aab7;
}


/* ==================== PROJECTS ==================== */

.projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 22px;
}

.project-card {
    overflow: hidden;
    border: 1px solid #2c2c2c;
    background: rgba(13, 16, 22, 0.82);
    transition: 0.3s ease;
}

.project-card:hover {
    transform: translateY(-5px);
    border-color: rgba(124, 92, 255, 0.48);
}

.project-featured {
    grid-column: 1 / -1;
}

.project-gallery {
    display: grid;
    gap: 5px;
    background: rgba(3, 5, 8, 0.72);
    border-bottom: 1px solid #2c2c2c;
}

.project-gallery.four-images {
    grid-template-columns: repeat(4, 1fr);
}

.project-gallery.two-images {
    grid-template-columns: repeat(2, 1fr);
}

.project-gallery.single-image {
    grid-template-columns: 1fr;
}

.gallery-image {
    position: relative;
    display: block;
    width: 100%;
    height: 230px;
    padding: 0;
    border: none;
    overflow: hidden;
    background: #0d1015;
    cursor: pointer;
}

.four-images .gallery-image {
    height: 230px;
}

.two-images .gallery-image {
    height: 260px;
}

.single-image .gallery-image {
    height: 300px;
}

.gallery-image::after {
    content: "VIEW";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(124,92,255,0.30), rgba(0,0,0,0.68));
    color: #ffffff;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 2px;
    opacity: 0;
    transition: 0.3s ease;
}

.gallery-image:hover::after {
    opacity: 1;
}

.gallery-image img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.gallery-image:hover img {
    transform: scale(1.04);
}

.project-content {
    padding: 28px;
}

.project-category {
    color: #00d9ff;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 2px;
    margin-bottom: 12px;
}

.project-content h3 {
    font-size: 1.35rem;
    line-height: 1.3;
    margin-bottom: 13px;
}

.project-content p {
    color: #a3aab7;
    margin-bottom: 20px;
}

.project-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
}


/* ==================== IMAGE MODAL ==================== */

.image-modal {
    position: fixed;
    inset: 0;
    z-index: 3000;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 30px;
    background: rgba(2, 3, 5, 0.96);
}

.image-modal.active {
    display: flex;
}

.modal-content {
    width: min(1200px, 95vw);
    max-height: 92vh;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.modal-content img {
    max-width: 100%;
    max-height: 82vh;
    object-fit: contain;
    border: 1px solid rgba(255, 255, 255, 0.09);
    background: linear-gradient(145deg, rgba(20, 23, 31, 0.92), rgba(10, 12, 17, 0.92));
}

.modal-content p {
    color: #cccccc;
    margin-top: 14px;
    text-align: center;
    font-size: 0.9rem;
}

.modal-close {
    position: absolute;
    top: 20px;
    right: 28px;
    width: 45px;
    height: 45px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 50%;
    background: linear-gradient(145deg, rgba(20, 23, 31, 0.92), rgba(10, 12, 17, 0.92));
    color: #ffffff;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
    transition: 0.3s ease;
}

.modal-close:hover {
    background: linear-gradient(135deg, #7c5cff, #b44dff 52%, #ff4ecd);
    color: #ffffff;
}


/* ==================== SKILLS ==================== */

.skills-list {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
}

.skills-list span {
    padding: 12px 18px;
    font-size: 0.9rem;
}


/* ==================== CONTACT ==================== */

.contact-section {
    text-align: center;
}

.contact-container {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.contact-section .section-heading {
    margin-bottom: 35px;
}

.contact-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    justify-content: center;
}


/* ==================== FOOTER ==================== */

footer {
    padding: 35px 0;
    background: rgba(3, 5, 8, 0.72);
}

.footer-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 25px;
}

footer strong {
    font-size: 1rem;
}

footer p {
    color: #737b8b;
    font-size: 0.8rem;
}

.footer-links {
    display: flex;
    gap: 20px;
}

.footer-links a {
    color: #929aaa;
    font-size: 0.85rem;
}

.footer-links a:hover {
    color: white;
}

.copyright {
    text-align: right;
}


/* ==================== MOBILE ==================== */

@media (max-width: 900px) {

    .hero-content,
    .about-grid {
        grid-template-columns: 1fr;
    }

    .hero-content {
        gap: 60px;
    }

    .hero {
        min-height: auto;
    }

    .hero-visual {
        justify-content: flex-start;
    }

    .profile-card {
        width: 100%;
        max-width: 380px;
    }
}


@media (max-width: 800px) {

    .menu-toggle {
        display: block;
    }

    nav {
        display: none;
        position: absolute;
        top: 76px;
        left: 0;
        width: 100%;
        padding: 25px 5%;
        flex-direction: column;
        align-items: flex-start;
        background: radial-gradient(circle at 10% 10%, rgba(124, 92, 255, 0.10), transparent 28%), radial-gradient(circle at 90% 20%, rgba(0, 210, 255, 0.08), transparent 25%), radial-gradient(circle at 70% 85%, rgba(255, 60, 190, 0.07), transparent 28%), #050608;
        border-bottom: 1px solid #292929;
    }

    nav.active {
        display: flex;
    }

    .services-grid,
    .projects-grid {
        grid-template-columns: 1fr;
    }

    .project-featured {
        grid-column: auto;
    }

    .project-gallery.four-images {
        grid-template-columns: repeat(2, 1fr);
    }

    .four-images .gallery-image {
        height: 220px;
    }

    .footer-container {
        flex-direction: column;
        align-items: flex-start;
    }

    .copyright {
        text-align: left;
    }
}


@media (max-width: 560px) {

    .section {
        padding: 80px 0;
    }

    .hero-content {
        padding: 80px 0;
    }

    .hero h1 {
        font-size: 2.8rem;
    }

    .hero-buttons .btn {
        width: 100%;
    }

    .about-highlight {
        grid-template-columns: 1fr;
    }

    .project-gallery.four-images,
    .project-gallery.two-images {
        grid-template-columns: 1fr;
    }

    .four-images .gallery-image,
    .two-images .gallery-image,
    .single-image .gallery-image {
        height: 260px;
    }

    .contact-buttons .btn {
        width: 100%;
    }

    .profile-image {
        height: 330px;
    }

    .image-modal {
        padding: 15px;
    }

    .modal-close {
        top: 10px;
        right: 12px;
    }
}/* ==================== PREMIUM UI LAYER ==================== */
body {
    position: relative;
    overflow-x: hidden;
}
body::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: -1;
    opacity: 0.24;
    background-image:
        linear-gradient(rgba(255,255,255,0.018) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.018) 1px, transparent 1px);
    background-size: 46px 46px;
    mask-image: linear-gradient(to bottom, black, transparent 88%);
}
.navbar {
    box-shadow: 0 10px 35px rgba(0,0,0,0.18);
}
.logo {
    background: linear-gradient(90deg, #ffffff, #9c82ff, #00d9ff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
nav a {
    position: relative;
}
nav a::after {
    content: "";
    position: absolute;
    left: 0;
    right: 100%;
    bottom: -8px;
    height: 2px;
    border-radius: 20px;
    background: linear-gradient(90deg, #7c5cff, #00d9ff);
    transition: right 0.25s ease;
}
nav a:hover::after {
    right: 0;
}
.nav-contact {
    background: rgba(124, 92, 255, 0.10);
    box-shadow: 0 8px 25px rgba(124,92,255,0.12);
}
.hero {
    position: relative;
    overflow: hidden;
}
.hero::before {
    content: "";
    position: absolute;
    width: 480px;
    height: 480px;
    right: -180px;
    top: -130px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(0,217,255,0.13), transparent 68%);
    pointer-events: none;
}
.hero::after {
    content: "";
    position: absolute;
    width: 420px;
    height: 420px;
    left: -200px;
    bottom: -200px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255,78,205,0.10), transparent 68%);
    pointer-events: none;
}
.hero-content {
    position: relative;
    z-index: 1;
}
.eyebrow,
.section-label {
    background: linear-gradient(90deg, #00d9ff, #7c5cff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
.hero h1 span {
    background: linear-gradient(100deg, #ffffff, #9c82ff 42%, #00d9ff 75%, #ff4ecd);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
.btn {
    border-radius: 12px;
    box-shadow: 0 10px 28px rgba(0,0,0,0.22);
}
.btn-primary {
    box-shadow: 0 12px 32px rgba(124,92,255,0.25);
}
.btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 18px 42px rgba(255,78,205,0.22);
}
.btn-secondary:hover {
    background: rgba(0,217,255,0.07);
    box-shadow: 0 12px 30px rgba(0,217,255,0.10);
}
.hero-tech span,
.project-tech span,
.skills-list span {
    background: rgba(124,92,255,0.055);
    border-color: rgba(124,92,255,0.18);
    transition: 0.25s ease;
}
.hero-tech span:hover,
.project-tech span:hover,
.skills-list span:hover {
    border-color: rgba(0,217,255,0.40);
    background: rgba(0,217,255,0.07);
    transform: translateY(-2px);
}
.profile-card {
    position: relative;
    border-color: rgba(255,255,255,0.10);
    border-radius: 20px;
    box-shadow:
        0 25px 70px rgba(0,0,0,0.48),
        0 0 60px rgba(124,92,255,0.07);
}
.profile-card::before {
    content: "";
    position: absolute;
    inset: -1px;
    border-radius: 20px;
    padding: 1px;
    background: linear-gradient(135deg, rgba(124,92,255,0.5), transparent 35%, rgba(0,217,255,0.35));
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    pointer-events: none;
}
.profile-image {
    border-radius: 14px;
}
.status span {
    background: #42f5a7;
    box-shadow: 0 0 12px rgba(66,245,167,0.65);
}
.section {
    position: relative;
}
.section-dark {
    background:
        radial-gradient(circle at 15% 25%, rgba(124,92,255,0.055), transparent 28%),
        radial-gradient(circle at 85% 75%, rgba(0,217,255,0.045), transparent 28%),
        rgba(9,11,15,0.76);
}
.section-heading h2 {
    background: linear-gradient(100deg, #ffffff, #d7d9df 55%, #9c82ff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
.about-highlight {
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 20px 55px rgba(0,0,0,0.24);
}
.highlight {
    transition: background 0.25s ease;
}
.highlight:hover {
    background: linear-gradient(145deg, rgba(124,92,255,0.10), rgba(0,217,255,0.04));
}
.service-card,
.project-card {
    border-radius: 18px;
    box-shadow: 0 18px 55px rgba(0,0,0,0.20);
}
.service-card:hover,
.project-card:hover {
    box-shadow:
        0 24px 65px rgba(0,0,0,0.34),
        0 0 35px rgba(124,92,255,0.06);
}
.service-card:nth-child(1):hover {
    border-color: rgba(124,92,255,0.48);
}
.service-card:nth-child(2):hover {
    border-color: rgba(0,217,255,0.45);
}
.service-card:nth-child(3):hover {
    border-color: rgba(255,78,205,0.45);
}
.service-card:nth-child(4):hover {
    border-color: rgba(66,245,167,0.42);
}
.project-card {
    position: relative;
}
.project-card::before {
    content: "";
    position: absolute;
    width: 260px;
    height: 260px;
    top: -180px;
    right: -100px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(124,92,255,0.12), transparent 68%);
    pointer-events: none;
}
.project-gallery {
    border-radius: 0;
}
.gallery-image {
    transition: filter 0.3s ease;
}
.gallery-image:hover {
    filter: brightness(1.08);
}
.project-category {
    background: linear-gradient(90deg, #00d9ff, #7c5cff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
.skills-list span {
    border-radius: 999px;
}
.contact-section {
    position: relative;
    overflow: hidden;
}
.contact-section::before {
    content: "";
    position: absolute;
    width: 520px;
    height: 520px;
    right: -230px;
    top: -240px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(124,92,255,0.15), rgba(255,78,205,0.05) 45%, transparent 70%);
    pointer-events: none;
}
.contact-container {
    position: relative;
    z-index: 1;
}
.contact-section .section-heading h2 {
    background: linear-gradient(100deg, #ffffff, #9c82ff, #00d9ff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}
.modal-close {
    border-radius: 50%;
    background: rgba(18,21,28,0.90);
    box-shadow: 0 10px 30px rgba(0,0,0,0.4);
}
.modal-close:hover {
    background: linear-gradient(135deg, #7c5cff, #ff4ecd);
    color: #ffffff;
    border-color: transparent;
}
footer {
    border-top: 1px solid rgba(255,255,255,0.06);
}
::-webkit-scrollbar {
    width: 9px;
}
::-webkit-scrollbar-track {
    background: #050608;
}
::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #7c5cff, #00d9ff);
    border-radius: 20px;
}
::selection {
    background: rgba(124,92,255,0.55);
    color: #ffffff;
}
@media (max-width: 800px) {
    nav {
        background: rgba(5,6,8,0.97);
        box-shadow: 0 20px 40px rgba(0,0,0,0.35);
    }
}
/* ==================== PREMIUM BACKGROUND PATTERN ==================== */
html {
    background: #07070a;
}
body {
    background:
        radial-gradient(circle at 15% 10%, rgba(88, 28, 135, 0.18), transparent 30%),
        radial-gradient(circle at 85% 18%, rgba(37, 99, 235, 0.15), transparent 28%),
        radial-gradient(circle at 50% 55%, rgba(6, 182, 212, 0.08), transparent 32%),
        #07070a;
    background-attachment: fixed;
}
body::before {
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    opacity: 0.34;
    background-image:
        linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
    background-size: 46px 46px;
    mask-image: linear-gradient(
        to bottom,
        rgba(0,0,0,0.9),
        rgba(0,0,0,0.45) 45%,
        transparent 100%
    );
}
.hero,
.section,
footer {
    position: relative;
}
.hero::before,
.section::before {
    content: "";
    position: absolute;
    width: 260px;
    height: 260px;
    border-radius: 50%;
    pointer-events: none;
    filter: blur(80px);
    opacity: 0.12;
}
.hero::before {
    top: 12%;
    right: 5%;
    background: #7c3aed;
}
.section:nth-of-type(2)::before {
    bottom: 10%;
    left: 3%;
    background: #06b6d4;
}
.section:nth-of-type(3)::before {
    top: 15%;
    right: 8%;
    background: #2563eb;
}
.section:nth-of-type(4)::before {
    bottom: 5%;
    left: 10%;
    background: #a855f7;
}
.btn-primary {
    background: linear-gradient(135deg, #7c3aed, #2563eb);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 8px 30px rgba(37, 99, 235, 0.18);
}
.btn-primary:hover {
    box-shadow: 0 12px 35px rgba(124, 58, 237, 0.28);
}
.btn-secondary:hover {
    border-color: #7c3aed;
    background: rgba(124, 58, 237, 0.08);
}
.nav-contact:hover {
    border-color: #7c3aed;
    background: rgba(124, 58, 237, 0.08);
}
.project-card:hover,
.service-card:hover {
    border-color: rgba(124, 58, 237, 0.55);
}
.hero-tech span:hover,
.project-tech span:hover,
.skills-list span:hover {
    border-color: rgba(37, 99, 235, 0.55);
    color: #ffffff;
}
/* ==================== PREMIUM PROJECT CARD LAYER ==================== */
.project-card {
    position: relative;
    border-color: rgba(255, 255, 255, 0.09);
    border-radius: 14px;
    background:
        linear-gradient(145deg, rgba(255,255,255,0.035), rgba(255,255,255,0.01)),
        rgba(12, 12, 16, 0.92);
    box-shadow:
        0 18px 50px rgba(0, 0, 0, 0.28),
        inset 0 1px 0 rgba(255, 255, 255, 0.025);
}
.project-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 8%;
    right: 8%;
    height: 1px;
    background: linear-gradient(
        90deg,
        transparent,
        rgba(124, 58, 237, 0.65),
        rgba(37, 99, 235, 0.65),
        transparent
    );
    opacity: 0.45;
    pointer-events: none;
}
.project-card:hover {
    transform: translateY(-7px);
    border-color: rgba(124, 58, 237, 0.42);
    box-shadow:
        0 24px 65px rgba(0, 0, 0, 0.42),
        0 0 35px rgba(37, 99, 235, 0.08);
}
.project-gallery {
    position: relative;
    background:
        radial-gradient(circle at 50% 50%, rgba(37, 99, 235, 0.08), transparent 55%),
        #08080b;
}
.gallery-image {
    transition:
        transform 0.35s ease,
        filter 0.35s ease;
}
.gallery-image::after {
    background:
        linear-gradient(
            135deg,
            rgba(124, 58, 237, 0.28),
            rgba(37, 99, 235, 0.30)
        );
    backdrop-filter: blur(2px);
}
.gallery-image:hover img {
    transform: scale(1.045);
    filter: brightness(1.08) saturate(1.08);
}
.project-content {
    position: relative;
}
.project-category {
    color: #8b7cf6;
}
.project-content h3 {
    letter-spacing: -0.4px;
}
.project-content p {
    color: #9999a3;
}
.project-tech span {
    border-color: rgba(255, 255, 255, 0.10);
    background: rgba(255, 255, 255, 0.025);
    transition:
        border-color 0.25s ease,
        color 0.25s ease,
        background 0.25s ease,
        transform 0.25s ease;
}
.project-tech span:hover {
    background: rgba(124, 58, 237, 0.08);
    border-color: rgba(124, 58, 237, 0.50);
    transform: translateY(-2px);
}
.project-featured {
    box-shadow:
        0 22px 65px rgba(0, 0, 0, 0.34),
        inset 0 1px 0 rgba(255, 255, 255, 0.025);
}
/* ==================== PREMIUM NAVIGATION LAYER ==================== */
.navbar {
    background:
        linear-gradient(
            90deg,
            rgba(7, 7, 10, 0.96),
            rgba(13, 13, 20, 0.94),
            rgba(7, 7, 10, 0.96)
        );
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.22);
}
.navbar::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    height: 1px;
    background: linear-gradient(
        90deg,
        transparent 5%,
        rgba(124, 58, 237, 0.55),
        rgba(37, 99, 235, 0.55),
        rgba(6, 182, 212, 0.40),
        transparent 95%
    );
    opacity: 0.7;
    pointer-events: none;
}
.logo {
    position: relative;
    letter-spacing: 1.4px;
    transition: 0.3s ease;
}
.logo::after {
    content: "";
    display: inline-block;
    width: 6px;
    height: 6px;
    margin-left: 7px;
    margin-bottom: 2px;
    border-radius: 50%;
    background: linear-gradient(135deg, #7c3aed, #06b6d4);
    box-shadow: 0 0 12px rgba(124, 58, 237, 0.55);
}
.logo:hover {
    transform: translateY(-1px);
}
nav {
    gap: 26px;
}
nav a {
    position: relative;
    color: #a7a7b0;
    transition:
        color 0.25s ease,
        transform 0.25s ease;
}
nav a:not(.nav-contact)::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -7px;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, #7c3aed, #2563eb);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 0.25s ease;
}
nav a:not(.nav-contact):hover {
    color: #ffffff;
    transform: translateY(-1px);
}
nav a:not(.nav-contact):hover::after {
    transform: scaleX(1);
}
.nav-contact {
    padding: 10px 18px;
    border: 1px solid rgba(124, 58, 237, 0.55);
    border-radius: 8px;
    background: linear-gradient(
        135deg,
        rgba(124, 58, 237, 0.12),
        rgba(37, 99, 235, 0.10)
    );
    box-shadow: 0 5px 20px rgba(37, 99, 235, 0.08);
}
.nav-contact:hover {
    color: #ffffff;
    border-color: rgba(124, 58, 237, 0.9);
    background: linear-gradient(
        135deg,
        rgba(124, 58, 237, 0.22),
        rgba(37, 99, 235, 0.18)
    );
    transform: translateY(-2px);
}
.menu-toggle {
    width: 44px;
    height: 44px;
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: 9px;
    background: rgba(255, 255, 255, 0.035);
    color: #ffffff;
    transition:
        border-color 0.25s ease,
        background 0.25s ease,
        transform 0.25s ease;
}
.menu-toggle:hover {
    border-color: rgba(124, 58, 237, 0.55);
    background: rgba(124, 58, 237, 0.10);
    transform: translateY(-1px);
}
@media (max-width: 800px) {
    nav {
        background:
            linear-gradient(
                145deg,
                rgba(10, 10, 15, 0.98),
                rgba(7, 7, 10, 0.98)
            );
        box-shadow: 0 18px 35px rgba(0, 0, 0, 0.35);
    }
    nav a {
        width: 100%;
        padding: 8px 0;
    }
    nav a:not(.nav-contact)::after {
        bottom: 0;
        left: 0;
        right: auto;
        width: 35px;
    }
    .nav-contact {
        width: auto;
        margin-top: 4px;
    }
}
/* ==================== PREMIUM HERO LAYER ==================== */
.hero {
    position: relative;
    overflow: hidden;
    background:
        radial-gradient(
            circle at 78% 45%,
            rgba(124, 58, 237, 0.075),
            transparent 32%
        );
}
.hero::after {
    content: "";
    position: absolute;
    width: 420px;
    height: 420px;
    top: 8%;
    right: -180px;
    border-radius: 50%;
    background:
        radial-gradient(
            circle,
            rgba(37, 99, 235, 0.14),
            rgba(124, 58, 237, 0.06) 38%,
            transparent 70%
        );
    filter: blur(20px);
    pointer-events: none;
}
.hero-content {
    position: relative;
    z-index: 1;
}
.eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 7px 13px;
    margin-bottom: 24px;
    border: 1px solid rgba(124, 58, 237, 0.25);
    border-radius: 999px;
    background: rgba(124, 58, 237, 0.055);
    color: #a99cf8;
    letter-spacing: 2.5px;
    box-shadow: 0 8px 25px rgba(124, 58, 237, 0.06);
}
.eyebrow::before {
    content: "";
    width: 7px;
    height: 7px;
    flex-shrink: 0;
    border-radius: 50%;
    background: linear-gradient(135deg, #7c3aed, #06b6d4);
    box-shadow: 0 0 12px rgba(124, 58, 237, 0.65);
}
.hero h1 {
    text-shadow: 0 8px 35px rgba(0, 0, 0, 0.35);
}
.hero h1 span {
    background: linear-gradient(
        100deg,
        #8b7cf6 0%,
        #5b8def 48%,
        #4cc9d9 100%
    );
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
}
.hero-description {
    color: #a9a9b4;
}
.hero-buttons {
    margin-top: 6px;
}
.btn {
    position: relative;
    overflow: hidden;
    border-radius: 9px;
}
.btn::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 70%;
    height: 100%;
    background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.16),
        transparent
    );
    transform: skewX(-20deg);
    transition: left 0.55s ease;
    pointer-events: none;
}
.btn:hover::before {
    left: 130%;
}
.btn-primary {
    background:
        linear-gradient(
            135deg,
            #7c3aed 0%,
            #4f46e5 48%,
            #2563eb 100%
        );
    box-shadow:
        0 10px 30px rgba(79, 70, 229, 0.20),
        inset 0 1px 0 rgba(255, 255, 255, 0.14);
}
.btn-primary:hover {
    transform: translateY(-3px);
    box-shadow:
        0 14px 38px rgba(79, 70, 229, 0.30),
        inset 0 1px 0 rgba(255, 255, 255, 0.18);
}
.btn-secondary {
    border-color: rgba(255, 255, 255, 0.15);
    background: rgba(255, 255, 255, 0.025);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.025);
}
.btn-secondary:hover {
    border-color: rgba(6, 182, 212, 0.55);
    background: rgba(6, 182, 212, 0.055);
}
.hero-tech {
    gap: 9px;
}
.hero-tech span {
    border-color: rgba(255, 255, 255, 0.09);
    background: rgba(255, 255, 255, 0.025);
    transition:
        transform 0.25s ease,
        border-color 0.25s ease,
        background 0.25s ease;
}
.hero-tech span:hover {
    transform: translateY(-2px);
    border-color: rgba(37, 99, 235, 0.48);
    background: rgba(37, 99, 235, 0.06);
    color: #d9e4ff;
}
.profile-card {
    position: relative;
    border-color: rgba(255, 255, 255, 0.10);
    border-radius: 18px;
    background:
        linear-gradient(
            145deg,
            rgba(255, 255, 255, 0.045),
            rgba(255, 255, 255, 0.012)
        ),
        rgba(12, 12, 16, 0.88);
    box-shadow:
        0 25px 70px rgba(0, 0, 0, 0.42),
        0 0 45px rgba(124, 58, 237, 0.07),
        inset 0 1px 0 rgba(255, 255, 255, 0.045);
}
.profile-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 12%;
    right: 12%;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(
        90deg,
        transparent,
        #7c3aed,
        #2563eb,
        #06b6d4,
        transparent
    );
    opacity: 0.65;
}
.profile-image {
    border-radius: 12px;
    background: #0c0c10;
    box-shadow:
        inset 0 0 0 1px rgba(255, 255, 255, 0.05),
        0 15px 35px rgba(0, 0, 0, 0.28);
}
.profile-image img {
    transition: transform 0.5s ease, filter 0.5s ease;
}
.profile-card:hover .profile-image img {
    transform: scale(1.025);
    filter: brightness(1.04) saturate(1.04);
}
.profile-info h3 {
    letter-spacing: -0.3px;
}
.status {
    color: #9999a5;
}
.status span {
    background: #22c55e;
    box-shadow: 0 0 12px rgba(34, 197, 94, 0.65);
}
@media (max-width: 900px) {
    .hero::after {
        width: 300px;
        height: 300px;
        right: -160px;
    }
}
@media (max-width: 560px) {
    .eyebrow {
        font-size: 0.68rem;
        letter-spacing: 2px;
    }
    .hero::after {
        opacity: 0.6;
    }
}
/* ==================== PREMIUM ABOUT SERVICES LAYER ==================== */
.about-grid {
    position: relative;
}
.about-text p {
    color: #a9a9b4;
}
.about-text p:first-child {
    color: #d4d4dc;
    font-size: 1.12rem;
}
.about-highlight {
    position: relative;
    overflow: hidden;
    border-color: rgba(255, 255, 255, 0.09);
    border-radius: 14px;
    background:
        linear-gradient(
            135deg,
            rgba(124, 58, 237, 0.08),
            rgba(37, 99, 235, 0.04),
            rgba(6, 182, 212, 0.035)
        ),
        rgba(10, 10, 14, 0.72);
    box-shadow:
        0 18px 45px rgba(0, 0, 0, 0.22),
        inset 0 1px 0 rgba(255, 255, 255, 0.03);
}
.about-highlight::before {
    content: "";
    position: absolute;
    width: 180px;
    height: 180px;
    top: -100px;
    right: -70px;
    border-radius: 50%;
    background: rgba(124, 58, 237, 0.12);
    filter: blur(45px);
    pointer-events: none;
}
.highlight {
    position: relative;
    background: rgba(17, 17, 22, 0.78);
    border-color: rgba(255, 255, 255, 0.07);
    transition:
        background 0.3s ease,
        transform 0.3s ease;
}
.highlight:hover {
    background: rgba(124, 58, 237, 0.065);
    transform: translateY(-2px);
}
.highlight strong {
    background: linear-gradient(90deg, #ffffff, #b8b5ff);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
}
.highlight span {
    color: #90909c;
}
/* ==================== SERVICES ==================== */
.services-grid {
    position: relative;
}
.service-card {
    position: relative;
    overflow: hidden;
    border-color: rgba(255, 255, 255, 0.085);
    border-radius: 14px;
    background:
        linear-gradient(
            145deg,
            rgba(255, 255, 255, 0.035),
            rgba(255, 255, 255, 0.008)
        ),
        rgba(12, 12, 16, 0.78);
    box-shadow:
        0 15px 40px rgba(0, 0, 0, 0.20),
        inset 0 1px 0 rgba(255, 255, 255, 0.025);
}
.service-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 28px;
    width: 42px;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, #7c3aed, #2563eb);
    opacity: 0.75;
}
.service-card::after {
    content: "";
    position: absolute;
    width: 150px;
    height: 150px;
    right: -90px;
    bottom: -90px;
    border-radius: 50%;
    background: rgba(37, 99, 235, 0.09);
    filter: blur(35px);
    pointer-events: none;
    transition: 0.35s ease;
}
.service-card:hover {
    transform: translateY(-7px);
    border-color: rgba(124, 58, 237, 0.42);
    background:
        linear-gradient(
            145deg,
            rgba(124, 58, 237, 0.065),
            rgba(37, 99, 235, 0.025)
        ),
        rgba(12, 12, 16, 0.86);
    box-shadow:
        0 24px 55px rgba(0, 0, 0, 0.30),
        0 0 35px rgba(124, 58, 237, 0.055);
}
.service-card:hover::after {
    transform: scale(1.25);
}
.service-number {
    color: #7771d8;
    font-weight: 700;
    letter-spacing: 1px;
}
.service-card h3 {
    letter-spacing: -0.4px;
}
.service-card p {
    color: #92929e;
}
.section-heading h2 {
    text-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
}
.section-heading > p:last-child {
    color: #9999a5;
}